# Chitrai AI Inference Microservice

Python + FastAPI service for orchestration of open-source models (Stable Diffusion + ControlNet).

## Structure
- `main.py`: FastAPI entrypoint.
- `pipelines/`: Model initialization, LoRA loading, and inference engines.
- `requirements.txt`: Python package dependencies.
- `tests/`: Pytest files.
